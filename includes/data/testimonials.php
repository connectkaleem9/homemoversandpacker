<?php
/**
 * Customer reviews.
 *
 * ============================================================================
 *  THIS FILE SHIPS EMPTY ON PURPOSE.
 *
 *  The testimonials section on the homepage does not render at all while this
 *  array is empty, and no Review or AggregateRating schema is emitted.
 *
 *  Only add reviews that a real customer actually left you — copy them from
 *  your Google Business Profile, WhatsApp, or a written review. Inventing them
 *  breaches Google's structured data guidelines (which can get your rich
 *  results removed and, for a business running Ads, is a policy problem), and
 *  UAE consumer protection law treats fabricated reviews as misleading
 *  advertising. It is also the single easiest thing for a competitor to report.
 * ============================================================================
 *
 * To add a review, uncomment the example below and replace every field:
 *
 *   'quote'  — the customer's own words. Trim for length if you must, but do
 *              not rewrite them.
 *   'name'   — how they are willing to be credited, e.g. 'Ahmed R.'
 *   'city'   — Dubai, Sharjah or Ajman
 *   'rating' — 1 to 5, as they actually gave it
 *   'date'   — YYYY-MM-DD, when the review was left
 *   'photo'  — optional filename in assets/images/testimonials/, only with
 *              their permission. Omit it and their initials are shown instead.
 *   'source' — optional, e.g. 'Google' — where the review came from
 */

declare(strict_types=1);

return [

    /*
    [
        'quote'  => 'Replace this with a real review, in the customer\'s own words.',
        'name'   => 'Customer Name',
        'city'   => 'Dubai',
        'rating' => 5,
        'date'   => '2026-01-15',
        'photo'  => '',
        'source' => 'Google',
    ],
    */

];
